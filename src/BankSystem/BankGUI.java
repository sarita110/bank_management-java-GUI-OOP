package BankSystem;
//different classes are imported from their packages so they can used in this class
import BankSystem.views.Home;
public class BankGUI {
    //main method is the starting point of our GUI
    public static void main(String[] args) {
        //Instance of Home class of views is created so its constructor is called
        Home home = new Home();
    }
}
